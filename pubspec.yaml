name: taskflow_executive
description: "TaskFlow Executive - App de agendamiento de tareas para equipos"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.1
  firebase_messaging: ^15.1.6
  firebase_storage: ^12.4.5

  # Image picking (profile photo)
  image_picker: ^1.1.2

  # State management
  provider: ^6.1.2

  # Notifications
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4

  # Calendar
  table_calendar: ^3.1.3

  # Charts
  fl_chart: ^0.69.2

  # Dates / formatting
  intl: ^0.20.2

  # Phone calls
  url_launcher: ^6.3.1

  # CSV export & sharing
  csv: ^6.0.0
  share_plus: ^10.1.4
  path_provider: ^2.1.5

  # Icons
  lucide_icons: ^0.257.0
  shared_preferences: ^2.5.5
  cloud_functions: ^5.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

# Sprint 7.3.2B — CheCu launcher icon, generated from the single official
# source asset (assets/branding/logo_checu.png). The project has no adaptive
# icon split (foreground/background) today, only legacy mipmap icons, so this
# stays legacy-only rather than inventing a foreground/background split that
# isn't part of the original asset. remove_alpha_ios/background_color_ios
# flatten the source's transparent rounded-corner padding onto the same
# institutional background color for iOS, which requires fully opaque icons.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/branding/logo_checu.png"
  remove_alpha_ios: true
  background_color_ios: "#F5F1E8"

# Sprint 7.3.2B/D — native boot splash. Fixed institutional background/logo,
# intentionally identical in color_dark/android_12 so it never depends on the
# device theme, signed-in user, or role (it renders before any of that is
# known). Web is left untouched here: out of this sprint's scope and avoids
# re-touching web/index.html, which Sprint 7.3.2A already edited for the
# visible app name.
#
# image/image_dark and android_12.image/image_dark point at *derived* copies
# of assets/branding/logo_checu.png (same pixels, just resized/padded), not a
# new logo: flutter_native_splash has no size/scale parameter — its own docs
# say the source "should be sized for 4x pixel density" because the on-screen
# logical size is computed as source_width / 4, so the original 1146px master
# (meant for app icons) renders at ~286dp/286pt and looks oversized. The
# 384x384 derivative below restores parity with BrandLogo's splash size (96).
# android_12 additionally requires real transparent padding around the
# visible content (Android masks the outer third of the icon on Android 12+),
# which the 1146px master doesn't have, so it uses a separate 1152x1152
# derivative with the logo confined to a 700x700 safe zone.
flutter_native_splash:
  color: "#F5F1E8"
  color_dark: "#F5F1E8"
  image: assets/branding/logo_checu_splash.png
  image_dark: assets/branding/logo_checu_splash.png
  android_12:
    color: "#F5F1E8"
    color_dark: "#F5F1E8"
    image: assets/branding/logo_checu_splash_android12.png
    image_dark: assets/branding/logo_checu_splash_android12.png
  android: true
  ios: true
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/branding/logo_checu.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

// Stub compiled on Android / iOS / desktop where dart:js_interop is unavailable.
// AppUpdateService guards every call with kIsWeb so these bodies are never reached.

bool get webSwUpdateWaiting => false;
Future<void> activateWebUpdate() async {}

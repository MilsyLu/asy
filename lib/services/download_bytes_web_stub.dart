// Non-web stub. Callers should use share_plus (Share.shareXFiles) instead
// on Android/iOS, where a native share/save sheet is the expected pattern.

import 'dart:typed_data';

void downloadBytes(Uint8List bytes, String filename, String mimeType) {}
